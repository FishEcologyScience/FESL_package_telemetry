% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_network_analysis.R
\name{network_plot}
\alias{network_plot}
\title{Plot Network Data}
\usage{
network_plot(
  data,
  Min.traffic = 1,
  shapefile = NA,
  xlim = NULL,
  ylim = NULL,
  line.min = 0.5,
  line.max = 2.5,
  plot.title = NULL,
  y.axis = "latitude",
  x.axis = "longitude",
  labels = FALSE,
  label.size = 2,
  label.transparency = 0.6,
  label.nudge = 0,
  ...
)
}
\arguments{
\item{data}{The list object created by the \code{network_summary()} function. Must
contain \code{plot.data} and \code{receiver.locations} dataframes.}

\item{Min.traffic}{Numeric. Minimum number of movements required to display an edge
between receivers. Default is 1.}

\item{shapefile}{An sf spatial object to overlay on the plot for geographic context.
Default is NA (no shapefile).}

\item{xlim}{Numeric vector. Optional x-axis limits (longitude). Default is NULL (auto-scaled).}

\item{ylim}{Numeric vector. Optional y-axis limits (latitude). Default is NULL (auto-scaled).}

\item{line.min}{Numeric. Minimum line width for movement edges. Default is 0.5.}

\item{line.max}{Numeric. Maximum line width for movement edges. Default is 2.5.}

\item{plot.title}{Character. Optional plot title. Default is NULL.}

\item{y.axis}{Character. Label for the y-axis. Default is "latitude".}

\item{x.axis}{Character. Label for the x-axis. Default is "longitude".}

\item{labels}{Logical. If TRUE, adds receiver name labels to the plot. Default is FALSE.}

\item{label.size}{Numeric. Text size for receiver labels. Default is 2.}

\item{label.transparency}{Numeric. Alpha transparency for labels (0-1). Default is 0.6.}

\item{label.nudge}{Numeric. Horizontal nudge distance for labels. Default is 0.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot object displaying the movement network. Nodes represent receivers
(colored by detection frequency using viridis scale), and edges represent movements
between receivers (width scaled by movement frequency).
}
\description{
Creates a ggplot2-based visualization of fish movement networks between
acoustic receiver stations. Displays movement frequencies as weighted edges and receiver
activity as colored nodes, with optional shapefile overlay for geographic context.
}
